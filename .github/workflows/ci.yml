name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-test:
    name: Build and test
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore DetectorEstafas.slnx

      - name: Build
        run: dotnet build DetectorEstafas.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test DetectorEstafas.Tests/DetectorEstafas.Tests.csproj --configuration Release --no-build --verbosity normal
